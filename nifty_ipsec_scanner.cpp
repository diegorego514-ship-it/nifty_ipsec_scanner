#include <fstream>
#include <ostream>
#include <sstream>
#include <syncstream>
#include <regex>
#include <iomanip>

int main(){
    
    int open_ports(0 > 25 > 260);
    if (open_ports + "number of open ports");
    // ========================================= //
    
    popen, "open_closed_ports";
    pclose, "close_opened_ports";

    pthread_attr_getscope, "open_close_ports"; // For communicating between devices on the network
    pthread_attr_getscope, "close_open_ports"; // For blocking the connection between close-to-open-ports

    #define FOPEN_PORT_SCAN
    FOPEN_PORT_SCAN open_ports;

    scanf, (open_ports);

    #define port_in_ports(scan_for_open_ports);

    port_in_ports(scan_for_open_ports());

    if(printf)

    printf("[!] Your Port is open and vulnerable to hackers");

    else;

    printf("[*] Recommendation - Regularly scan your ports and remember to close them out properly, also from now on update your devices from time to time in order to avoid getting caught by vulnerabilities: ");

}
